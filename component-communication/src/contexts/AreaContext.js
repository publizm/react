import { createContext } from 'react';

// const area = {
//   nation: 'KOR',
//   region: 'Seoul',
// };

// const AreaContext = createContext(area);
const AreaContext = createContext();

export default AreaContext;
